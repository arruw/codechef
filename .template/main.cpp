#include <bits/stdc++.h>
using namespace std;

void solve()
{
    ;
}

int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int T = 0;
    cin >> T;
    for (auto x = 1; x <= T; x++)
    {
        solve();
    }

    return 0;
}